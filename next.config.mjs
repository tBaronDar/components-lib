/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["images.pexels.com", "images.unsplash.com"], // Add any additional domains you need
	},
};

export default nextConfig;
